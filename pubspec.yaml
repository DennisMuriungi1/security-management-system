name: final_test
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Firebase dependencies
  firebase_core: ^4.1.1
  firebase_auth: ^6.1.0
  cloud_firestore: ^6.0.2   # (optional) for storing user roles
  intl: ^0.20.2

  # UI icons
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.2
  firebase_storage: ^13.0.2
  image_picker: ^1.2.0
  fl_chart: ^1.1.1
  video_player: ^2.10.0
  chewie: ^1.13.0
  flutter_vlc_player: ^7.4.4
  mjpeg_stream: ^1.0.1
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Uncomment and add your image paths here
  # assets:
  #   - assets/images/

  # Uncomment if you add custom fonts
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
